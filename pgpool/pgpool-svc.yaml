apiVersion: v1
kind: Service
metadata:
  name: pgpool-ha-test-svc-ha-test
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: pg-ha-test
      port: 5432
      targetPort: postgresql
      protocol: TCP
      nodePort: null
  selector:
    app: pgpool-ha-test