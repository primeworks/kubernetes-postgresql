apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-postgres
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: postgres.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-postgres
            port:
              number: 8069