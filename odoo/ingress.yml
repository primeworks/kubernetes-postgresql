apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: od-ha-test
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: od-ha-test.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: od-ha-test
            port:
              number: 8069
  tls:
  - hosts:
    - od-ha-test.webdiscode.com
    secretName: od-ha-test.webdiscode.com-tls