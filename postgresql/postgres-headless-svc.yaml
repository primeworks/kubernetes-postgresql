apiVersion: v1
kind: Service
metadata:
  name: postgres-ha-test-headless-svc
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: postgres-ht
    port: 5432
    targetPort: postgresql
    protocol: TCP 
  selector:
    app: postgres